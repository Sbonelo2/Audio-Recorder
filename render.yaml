services:
  - type: web
    name: audio-recorder-web
    buildCommand: npx expo export:web
    staticPublishPath: dist
    routes:
      - type: rewrite
        source: /.*
        destination: /index.html
    envVars:
      - key: NODE_ENV
        value: production
